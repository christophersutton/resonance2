# CORS Configuration
# Use * for development to allow all origins
# For production, specify your frontend URL(s)
CORS_ORIGIN=*

# Database Configuration
DB_PATH=./db/data.db

# Authentication Configuration
# Secret key used to sign JWT tokens - use a strong random value in production
JWT_SECRET=your-jwt-secret-key-here

# Mailgun Configuration for Email Services
MAILGUN_API_KEY=your-mailgun-api-key
MAILGUN_DOMAIN=your-mailgun-domain.com

# Application URL
# Used for email links and other external references
# For development: http://localhost:3000
# For production: https://your-domain.com
APP_URL=http://localhost:3000